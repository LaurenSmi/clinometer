import MeasurementScreen from "@/components/measurement-screen";

export default function BaseMeasurementScreen() {
  return (
    <MeasurementScreen text="Hold your phone vertically so the camera is eye level and press Capture." />
  );
}
